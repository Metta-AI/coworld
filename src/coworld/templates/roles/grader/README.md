# Grader Template

The grader is a one-shot process that consumes an episode bundle and writes one grade JSON artifact.

Contract reference: `coworld/docs/roles/GRADER.md`.

Files:

- `grader.py` - bundle reader and grade writer scaffold.
- `Dockerfile` - minimal image shape for packaging the grader runnable.
