# AGENTS.md - Generic Optimizer Learnings

Start from evidence before editing.

State the failing capability in concrete terms.

Change the smallest owner of that capability.

Add the narrowest regression that would have caught the failure.

Re-run the same check before broadening scope.

Do not confuse creation with validation.

Keep provenance and distinguish confirmed facts from inferred or
session-derived lessons.
